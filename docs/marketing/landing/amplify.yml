version: 1
applications:
  - appRoot: docs/marketing/landing
    frontend:
      phases:
        build:
          commands: []
      artifacts:
        baseDirectory: .
        files:
          - '**/*'